# MongoDB — runs inside the container on localhost
MONGO_APP_USER=vsapp
MONGO_APP_PASS=vspassword123
MONGO_URI=mongodb://vsapp:vspassword123@localhost:27017/vulnsamurai

# JWT — CHANGE THIS to output of: openssl rand -hex 64
JWT_SECRET=changeme_run_openssl_rand_hex_64_and_paste_here
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# Services — both on localhost inside the container
BACKEND_URL=http://localhost:8000
SESSION_SECRET=vulnsamurai_session_secret_change_me
PORT=3000
